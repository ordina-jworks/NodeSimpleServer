/**
 * Enum that contains the different types of Workers that can be targeted for messaging.
 */
export enum MessageTarget {
    HTTP_WORKER     = 1,
    DATA_BROKER     = 2,
    INTERVAL_WORKER = 3
}